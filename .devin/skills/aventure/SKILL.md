# aventure

Use the public aVenture API.

- Install the CLI: `npm install -g @aventurevc/aventure-cli`
- Browse commands: `aventure help`
- Read the API docs at https://github.com/aventurevc/aventure-api/tree/main/docs
- This skill does not list endpoints; use `aventure help` and the docs to discover the current public surface.
